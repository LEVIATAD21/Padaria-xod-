{
  "name": "padaria-xodo",
  "private": true,
  "packageManager": "pnpm@11",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^24",
    "prettier": "^3",
    "turbo": "^2",
    "typescript": "^5"
  }
}
